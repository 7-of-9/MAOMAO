// import { fromJS } from 'immutable';
// import { makeSelectExtensionDomain } from '../selectors';

// const selector = makeSelectExtensionDomain();

describe('makeSelectExtensionDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
